{
    "$schema": "https://raw.githubusercontent.com/jetify-com/devbox/0.14.2/.schema/devbox.schema.json",
    "packages": ["go-task@latest"],
    "shell": {
        "init_hook": ["source ./devbox/init.sh"],
        "scripts": {
            "test": ["echo \"Error: no test specified\" && exit 1"]
        }
    }
}
